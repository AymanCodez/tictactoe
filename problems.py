# loop_number = int(input())
# names = [input().split(" ") for i in range(loop_number)]
# winners = [name[0] for name in names if name[1] == 'win']
# print(f'{winners}\n{len(winners)}')

# def compare(x):
#     return all(element == x[0] for element in x)
